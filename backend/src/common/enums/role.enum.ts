/**
 * Roles available in the system. USER can browse and purchase vehicles.
 * ADMIN can additionally create, update, delete and restock vehicles.
 */
export enum Role {
  USER = 'user',
  ADMIN = 'admin',
}
